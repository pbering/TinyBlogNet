@model MvcBlog.Models.HomeModel
<div class="posts">
    @foreach (var post in @Model.Posts)
    {
        <h1 class="post-title">
            <a href="@post.Url">@post.Title</a>
        </h1>
        <p>@post.Summary</p>
        @Html.Partial("~/Views/Shared/_Tagged.cshtml", post)
    }
</div>