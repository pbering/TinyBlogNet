@using TinyBlogNet
@model Post
<code>
    Posted @Model.Published.ToHumaneString(), tagged:
    @for (int i = 0; i < @Model.Tags.Count; i++)
    {
        var tag = @Model.Tags[i];

        <a href="@tag.Url">@tag.Name</a>@(i < @Model.Tags.Count - 1 ? ", " : "")
    }
</code>